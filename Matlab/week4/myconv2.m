function y = myconv(h,x)
    L = length(h); M = length(x);
    y = zeros(1, M+L);
    
%     for n = 1: length(y)
%         yn = 0;   
%         for m = 1:min(-L+n+1,M+1)
%                 yn = yn + h(m)*x(n-m);
%         end
%         y(n) = yn;
%     end


end